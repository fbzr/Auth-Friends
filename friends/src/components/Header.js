import React from 'react';
import { Typography } from '@material-ui/core';

const Header = () => {
    return (
        <div>
            <Typography variant='h2' component='h1'>Friends</Typography>
        </div>
    )
}

export default Header
